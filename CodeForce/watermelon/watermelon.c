#include <stdio.h>

int main ()  {
    int peso = 0; 
    scanf("%d", &peso);

    if (((peso % 2) == 0) || (peso = 2))
    {
        printf("YES");
    }
    else {
        printf("NO");
    }
    
}